spec:
  template:
    metadata:
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "poc-04-00"
        vault.hashicorp.com/agent-inject-secret-readme: "hello/poc"
