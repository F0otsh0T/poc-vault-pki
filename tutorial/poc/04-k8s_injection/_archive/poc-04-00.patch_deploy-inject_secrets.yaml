spec:
  template:
    metadata:
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "poc-04-00"
        vault.hashicorp.com/agent-inject-secret-kv: "hello/poc"
        vault.hashicorp.com/agent-inject-secret-demo.crt: "pki_int/issue/demo-svc-cluster-local"
        vault.hashicorp.com/agent-inject-template-demo.crt: |
          {{- with secret "pki_int/issue/demo-svc-cluster-local" "common_name=demo.svc.cluster.local" -}}
          {{ .Data.crtificate }}
          {{- end }}
    spec:
      serviceAccountName: poc-04-00