spec:
  template:
    metadata:
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "poc-04-01"
        vault.hashicorp.com/agent-inject-secret-ingest_me.txt: "hello/poc"
    spec:
      serviceAccountName: poc-04-01